<?php

  /**
   * @file
   * This is the Calendar module for calendar filtering and display in views.
   */

  use Drupal\Core\Url;
  use Drupal\views\ViewExecutable;
  use Drupal\Core\Routing\RouteMatchInterface;
  use Drupal\calendar\CalendarHelper;

  define('VIEWS_ADMINTOOLS', 0);

  function views_admintools_theme($path) {




    return [
      'vat_header' => [
        'render element' => 'view',
        'template' => 'vat-header',
        'variables' => array('vat' => [] ),
      ],
    ];
  }


  $views_admintools_kint_count = 0;
function views_admintools_preprocess_page(&$variables) {
  global $views_admintools_kint_count;
  if ( $views_admintools_kint_count++ < 1 ) {
 //   var_dump($row);
  }
}